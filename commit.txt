refactor: Phase 3 architectural decoupling and RCE security patch

This commit represents the culmination of Phase 3, encompassing massive architectural improvements and a critical security patch to completely secure the application from Arbitrary Code Execution (RCE).

Security Patch (Phase 3.1):
- Discovered and eliminated a critical RCE vulnerability in AssemblyLoadContext where scanning third-party server mods dynamically executed their Activator.CreateInstance payloads.
- Replaced dynamic assembly loading with Mono.Cecil.
- ServerModExtractor now performs pure static IL bytecode analysis to extract metadata (GUID, Name, Author, Version) without executing any untrusted code.

Architectural Decoupling:
- ApplicationService: Extracted low-level initialization (paths, legacy API keys) into InitializationService. Removed all instances of generic exception swallowing (fail loudly).
- ModScannerService: Decoupled the god-class into three distinct, interface-driven components: PluginMetadataExtractor, ServerModExtractor, and MisplacedModDetector.
- SpectreModCheckReporter: Sliced the monolithic 1200+ line UI facade into specialized renderers: ITableRenderer, IProgressRenderer, and ITextRenderer.

Performance & Caching:
- ModReconciliationService: Replaced the O(N*M) nested loops with highly optimized HashSet and Dictionary inverted indexes, bringing lookup complexity down to O(N+M) while preserving exact matching priorities.
- Decorator Caching: Completely eradicated IMemoryCache from ForgeApiService and ModDependencyService. Created CachedForgeApiService and CachedModDependencyService decorators to cleanly wrap the stateless base services with deterministic caching logic.

Testing & QA:
- Expanded the automated test suite from 180 to 207 rigorous tests.
- Re-implemented E2E integration tests using Roslyn to dynamically compile dummy .dll files on the fly into an isolated SptSandboxFixture.
- Enforced strict project rules (block-bodies, specific exception handling, XML inheritdocs).

Documentation:
- Updated README.md to publicly document the RCE vulnerability and the Mono.Cecil patch.
